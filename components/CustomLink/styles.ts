import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { 
        color: '#F08000', 
        fontWeight: '600',
        fontSize: 18,
        fontFamily: 'Poppins-Black',
        textAlign: 'center'
    }
});