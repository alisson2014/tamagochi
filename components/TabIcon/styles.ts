import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 8
    },
    image: {
        width: 24,
        height: 24
    },
    iconName: {
        fontSize: 12,
        lineHeight: 16
    }   
});